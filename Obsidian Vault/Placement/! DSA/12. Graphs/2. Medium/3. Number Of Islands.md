Given a grid of size N x M (N is the number of rows and M is the number of columns in the grid) consisting of '0's (Water) and ‘1's(Land). Find the number of **islands**.

An island is surrounded by water and is formed by connecting adjacent lands horizontally or vertically or diagonally i.e., in all 8 directions.

Examples:

![](https://static.takeuforward.org/content/ProblemSetter-cQJHsEaI)

### Intuition:

**How to solve this problem using a graph?**  
Think of all the cells in the grid as nodes or vertices which are connected through each other via 8 edges.

**How to traverse the neighbours efficiently?**  
The 8 neighbours of the current cell can be shown like this:  
  
![](https://static.takeuforward.org/premium/Graphs/Traversal%20Problems/Number%20of%20islands/nieghbors-PCKbhKGS)  
It is clear from the above image that:

- row can be row-1, row, row+1. Or deltaRow varies frmom -1 to 1.
- col can be col-1, col, col+1. Or deltaCol varies frmom -1 to 1.

  
Therefore, to effectively traverse all the neighbors, two loops can be used.

### Approach:

1. Determine the dimensions of grid. Create a 2D visited array and initialize all values as false. Initialize a counter for the number of islands.
2. Loop through each cell in the grid. If the cell is land and not yet visited, it signifies the start of a new island.
3. Use BFS to explore all connected land cells starting from this cell and mark them visited.Increase the island count after completing the BFS for the current island.

#### Dry Run:

**Consider the following example:**  
  
![](https://static.takeuforward.org/premium/Graphs/Traversal%20Problems/Number%20of%20islands/givenMatrix-DixN20ob)  

![Image 1](https://static.takeuforward.org/premium/Graphs/Traversal%20Problems/Number%20of%20islands/1.png-flORUeXo)

![Image 2](https://static.takeuforward.org/premium/Graphs/Traversal%20Problems/Number%20of%20islands/2.png-G4oBFz-N)

![Image 3](https://static.takeuforward.org/premium/Graphs/Traversal%20Problems/Number%20of%20islands/3.png-gfLd9_hI)

![Image 4](https://static.takeuforward.org/premium/Graphs/Traversal%20Problems/Number%20of%20islands/4.png-Vm7gf6By)

![Image 5](https://static.takeuforward.org/premium/Graphs/Traversal%20Problems/Number%20of%20islands/5.png-w97WAV07)

![Image 6](https://static.takeuforward.org/premium/Graphs/Traversal%20Problems/Number%20of%20islands/6.png-yPLCebOD)

![Image 7](https://static.takeuforward.org/premium/Graphs/Traversal%20Problems/Number%20of%20islands/7.png-MVUbvqF8)

![Image 8](https://static.takeuforward.org/premium/Graphs/Traversal%20Problems/Number%20of%20islands/8.png-M4LJ3ZMq)

![Image 9](https://static.takeuforward.org/premium/Graphs/Traversal%20Problems/Number%20of%20islands/9.png-URuS4q2X)

![Image 10](https://static.takeuforward.org/premium/Graphs/Traversal%20Problems/Number%20of%20islands/10.png-dwEku1h-)

![Image 11](https://static.takeuforward.org/premium/Graphs/Traversal%20Problems/Number%20of%20islands/11-83Er2daj)

1/11

![](https://static.takeuforward.org/premium/Graphs/Traversal%20Problems/Number%20of%20islands/givenMartix-yBFPI9WI)  
Here, the number of islands are 3.

```cpp
class Solution {
private:
    
    /* Function to determine if the cell
     is valid (within grid's boundaries) */
    bool isValid(int i, int j, int n, int m) {
        if (i < 0 || i >= n) return false;
        if (j < 0 || j >= m) return false;
        
        // Return true if cell is valid
        return true;
    }
    
    void bfs(int i, int j, vector<vector<bool>>& vis, 
             vector<vector<char>>& grid) {

        // Mark the node as visited
        vis[i][j] = true;
        
        // Queue required for BFS traversal
        queue<pair<int, int>> q;
        
        // push the node in queue
        q.push({i, j});
        
        // Dimensions of grid
        int n = grid.size();
        int m = grid[0].size();
        
        // Until the queue becomes empty
        while (!q.empty()) {
            // Get the cell from queue
            pair<int, int> cell = q.front();
            q.pop();
            
            // Determine its row & column
            int row = cell.first;
            int col = cell.second;
            
            // Traverse the 8 neighbours
            for (int delRow = -1; delRow <= 1; delRow++) {
                for (int delCol = -1; delCol <= 1; delCol++) {
                    // Coordinates of new cell
                    int newRow = row + delRow;
                    int newCol = col + delCol;
                    
                    /* Check if the new cell is valid, 
                    unvisited, and a land cell */
                    if (isValid(newRow, newCol, n, m) 
                        && grid[newRow][newCol] == '1' 
                        && !vis[newRow][newCol]) {
                            
                        // Mark the node as visited
                        vis[newRow][newCol] = true;
                        
                        // Push new cell in queue
                        q.push({newRow, newCol});
                    }
                }
            }
        }
    }
    
public:
    // Function to find the number of islands in given grid
    int numIslands(vector<vector<char>>& grid) {
        // Size of the grid
        int n = grid.size();
        int m = grid[0].size();
        
        // Visited array
        vector<vector<bool>> vis(n, vector<bool>(m, false));
        
        // To store the count of islands
        int count = 0;
        
        // Traverse the grid
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < m; j++) {
                /* If not visited and is a land, 
                start a new traversal */
                if (!vis[i][j] && grid[i][j] == '1') {
                    count++;
                    bfs(i, j, vis, grid);
                }
            }
        }
        
        return count;
    }
};
```