

```cpp
// Online C++ compiler to run C++ program online
#include <iostream>
#include <sstream>
#include <string.h>
using namespace std;

int getoctval(const string &something){
    int value = 0;
    if(something[0] == 'r') value += 1;
    if(something[1] == 'w') value += 2;
    if(something[2] == 'x') value += 4;
    return value;
}

void stringsort(string s){
    string ans = "";
    s = s.substr(6);
    stringstream ss(s);
    string o_thing;
    string g_thing;
    string u_thing;
    
    ss >> o_thing >> g_thing >> u_thing;
    cout << o_thing << " " << g_thing << " " << u_thing << endl;
    
    o_thing = o_thing.substr(2);
    g_thing = g_thing.substr(2);
    u_thing = u_thing.substr(2);
    
    int ov = getoctval(o_thing);
    int gv = getoctval(g_thing);
    int uv = getoctval(u_thing);
    cout << ov << gv << uv << endl;
}

int main() {
    // Write C++ code here
    std::cout << "Try programiz.pro " << "or not" << endl;
    string s = "chmod o-r-x g-rwx u-rw-";
    stringsort(s);
    return 0;
}
```