### My Code
```cpp
#include <bits/stdc++.h>
using namespace std;

int main(){
	string s = "hello";

	stack<char> st;

	for (char c : s){
		st.push(c);
	}

	while(st.empty() != 1){
		s.push_back(s.top());
		s.pop();
	}

	return 0;
}
```