```py
choosing[i]=true;

number[i]=1+max(number[1..n]);

choosing[i]=false;

for each process j:

 while(choosing[j]);

 while(number[j]!=0 &&
      (number[j],j)<(number[i],i));

Critical Section
number[i]=0;
```
