|ackage/Method|Description|Code Example|
|---|---|---|
|bandit|Bandit is a tool designed to find common security issues in Python code. To do this, Bandit processes each file, builds an AST from it, and runs appropriate plugins against the AST nodes. Once Bandit has finished scanning all the files, it generates a report.|Example: Install Bandit:<br><br>1. 1<br><br>1. `pip install bandit`<br><br>Copied!Wrap Toggled!<br><br>Run Bandit:<br><br>1. 1<br><br>1. `bandit -r ~/your_repos/project`<br><br>Copied!Wrap Toggled!<br><br>The `-r` flag indicates that Bandit should run recursively through all files and subdirectories in the specified directory.|
|bash|Bash, or the Bourne Again Shell command, is a command-line interpreter commonly used in Unix-based operating systems. It runs in a text window where the user can interpret commands to carry out various actions.|Example: This generates a list of numbers and prints them:<br><br>1. 1<br>2. 2<br>3. 3<br>4. 4<br>5. 5<br><br>1. `#!/bin/bash`<br>2. `# Loop from 1 to 3 and print the numbers`<br>3. `for i in {1..3}; do`<br>4.   `echo "Number: $i"`<br>5. `done`<br><br>Copied!Wrap Toggled!|
|cd|Used to move efficiently from the existing working directory to different directories on your system.|1. 1<br><br>1. `cd [directory]`<br><br>Copied!Wrap Toggled!|
|cursor. execute(operation, params=None, multi=False)|This method executes the given database operation (query or command). The parameters found in the tuple or dictionary params are bound to the variables in the operation.|1. 1<br><br>1. `cursor.execute ("<SQL Query>")`<br><br>Copied!Wrap Toggled!<br><br>Example:<br><br>1. 1<br><br>1. `cursor.execute("SELECT * FROM table_name")`<br><br>Copied!Wrap Toggled!|
|echo|Allows users to display lines of text or strings that are passed as arguments.|Syntax:<br><br>1. 1<br><br>1.     `echo [option] [string]`<br><br>Copied!Wrap Toggled!<br><br>Example:<br><br>1. 1<br><br>1. `echo "Hello, world!" # This prints Hello, world!`<br><br>Copied!Wrap Toggled!|
|export|Given directly to the shell (for example, bash ) to tell it to add or modify one of its environment variables. It is used to ensure the environment variables and functions are passed to child processes.|Syntax:<br><br>1. 1<br><br>1. `export - export [-f] [-n] [name[=value] ...] or export -p`<br><br>Copied!Wrap Toggled!<br><br>Examples:<br><br>1. 1<br>2. 2<br>3. 3<br><br>1. `export PATH="$PATH:/path/to/new/directory" #Setting PATH variable`<br>2. `export PYTHONPATH="/path/to/python/modules"`<br>3. `export PYTHONSTARTUP="/path/to/python/startup/script.py" #Setting Python Environment Variables`<br><br>Copied!Wrap Toggled!|
|git clone|You can create a copy of a specific repository or branch within a repository.|1. 1<br><br>1. `git clone REPOSITORY_URL [DESTINATION_DIRECTORY]` <br><br>Copied!Wrap Toggled!|
|hvac|HashiCorp Vault API client for Python 3.x. Tested against the latest release, HEAD ref, and 3 previous minor versions (counting back from the latest release) of Vault.|Examples: (for `Python`)<br><br>1. 1<br>2. 2<br>3. 3<br><br>1. `import hvac`<br>2. `# Create a client object`<br>3. `client = hvac.Client(url='http://localhost:8200',oken='your_vault_token')`<br><br>Copied!Wrap Toggled!|
|meta|HTML tags are used to provide additional information about a page to search engines and other clients.|Examples:<br><br>1. 1<br>2. 2<br>3. 3<br><br>1. `<meta name="title" content="Page Title"> #Title meta tag`<br>2. `<meta name="description" content="Page description goes here."> #Description Meta tag		<meta name="keywords" content="keyword1, keyword2, keyword3"> #Keywords meta tag`<br>3. `<meta name="viewport" content="width=device-width, initial-scale=1.0"> #viewport meta tag`<br><br>Copied!Wrap Toggled!|
|pip|To make sure that requests will function, the pip program searches for the package in the Python Package Index (PyPI), resolves any dependencies, and installs everything in your current Python environment.|1. 1<br><br>1. `pip list`<br><br>Copied!Wrap Toggled!|
|pip install|The pip install <package> command looks for the latest version of the package and installs it.|1. 1<br><br>1. `pip install example_package`<br><br>Copied!Wrap Toggled!|
|python3|Python 3 is a widely used programming language known for its readability and versatility.|1. 1<br>2. 2<br>3. 3<br><br>1. `sudo apt-get update`<br>2. `sudo apt-get install python3`<br>3. `python3 --version #to verify the python version`<br><br>Copied!Wrap Toggled!|
|script|This tag is used to embed a client-side script (JavaScript). The **script** element either contains scripting statements or points to an external script file through the src attribute.|Examples:<br><br>Inline JavaScript Example<br><br>1. 1<br>2. 2<br>3. 3<br>4. 4<br>5. 5<br>6. 6<br>7. 7<br>8. 8<br><br>1. `<script>`<br>2.     `function function_name(name) {`<br>3.         `alert("Hello, " + name + "!");`<br>4.     `}`<br><br>6.     `Function_name("Alice");`<br>7. `</script>`<br><br>Copied!Wrap Toggled!<br><br>External Script Example<br><br>1. 1<br><br>1. `<script src="myscript_name.js"></script>`<br><br>Copied!Wrap Toggled!|
|sudo apt|sudo apt-get update and sudo apt-get upgrade are two commands you can use to keep all of your packages up to date in Debian or a Debian-based Linux distribution.|Syntax:<br><br>1. 1<br>2. 2<br>3. 3<br>4. 4<br>5. 5<br><br>1. `sudo apt update`<br>2. `or`<br>3. `sudo apt-get update`<br>4. `or`<br>5. `sudo apt-get update && sudo apt-get upgrade`<br><br>Copied!Wrap Toggled!|
|vault|vault-cli is a Python 3.6+ tool that offers simple interactions to manipulate secrets from HashiCorp Vault. This command shows a list of commands you can use with the tool.|Examples: (for `Bash`)<br><br>1. 1<br>2. 2<br>3. 3<br>4. 4<br>5. 5<br>6. 6<br>7. 7<br>8. 8<br><br>1. `vault operator init #initialize vault`<br>2. `vault login <token> #Login and authenticate`<br><br>4. `vault help`<br>5. `or`<br>6. `vault-cli --help`<br><br>8. `# command to see available options and subcommands.`<br><br>Copied!Wrap Toggled!|
|VAULT_ADDR|Address of the Vault server expressed as a URL and port, for example, https://127.0.0.1:8200/.|Example:<br><br>1. 1<br><br>1. `export VAULT_ADDR=https://127.0.0.1:8200`<br><br>Copied!Wrap Toggled!<br><br>This command sets the `VAULT_ADDR` variable to the URL of your Vault server, which is typically running on port 8200 by default. Make sure to replace [https://vault.example.com:8200](https://vault.example.com:8200/) with the actual URL of your HashiCorp Vault server.|
|VAULT_TOKEN|Vault authentication token. Conceptually similar to a session token on a website, the VAULT_TOKEN environment variable holds the contents of the token.|Example:<br><br>1. 1<br><br>1. `export VAULT_TOKEN=my-secret-token-value`<br><br>Copied!Wrap Toggled!<br><br>The `VAULT_TOKEN` environment variable holds the actual contents of the authentication token.|
|vault server|Starts a Vault server that responds to API requests.|Syntax:<br><br>1. 1<br>2. 2<br><br>1. `vault server`<br>2. `vault server -config=/path/to/config.hcl`<br><br>Copied!Wrap Toggled!<br><br>Once the `Vault server` is running, you can access the Vault UI by opening a web browser and navigating to the appropriate address, typically [http://127.0.0.1:8200](http://127.0.0.1:8200/), or as configured in your `vault.hcl` file.|
|wget|Stands for web get. The wget is a free non-interactive file downloader command. Non-interactive means it can work in the background when the user is not logged in.|1. 1<br><br>1. `wget 'https://www.example.com/somefile.txt'`|