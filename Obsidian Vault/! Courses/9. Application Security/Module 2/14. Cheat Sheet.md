|Package/Method|Description|Code Example|
|---|---|---|
|bash|Bash, or the Bourne Again Shell command, is a command-line interpreter commonly used in Unix-based operating systems. It runs in a text window where the user can interpret commands to carry out various actions.|Example: This generates a list of numbers and prints them:<br><br>1. 1<br>2. 2<br>3. 3<br>4. 4<br>5. 5<br><br>1.  `#!/bin/bash`<br>2.  `# Loop from 1 to 3 and print the numbers`<br>3.  `for i in {1..3}; do`<br>4.     `echo "Number: $i"`<br>5. `done`<br><br>Copied!Wrap Toggled!|
|alias|Lets you create a shortcut name for a command, file name, or any shell text. Using aliases saves a lot of time when performing frequent tasks.|Basic syntax of `alias` command<br><br>1. 1<br><br>1. `alias [new-name[=command]`<br><br>Copied!Wrap Toggled!<br><br>Example 1: Replaces command `cd C:\Users\Videos` with new alias `cdv`; so instead of typing `cd C:\Users\Videos`, one can type `cdv` to execute the same command<br><br>1. 1<br>2. 2<br><br>1. `alias cdv="cd C:\Users\Videos"`<br>2. `cdv`<br><br>Copied!Wrap Toggled!<br><br>Example 2: Use `-p` option to view all your alias commands<br><br>1. 1<br><br>1. `alias -p`<br><br>Copied!Wrap Toggled!<br><br>Example 3: Use unalias with `-a` option to remove all your alias commands<br><br>1. 1<br><br>1. `unalias -a`<br><br>Copied!Wrap Toggled!<br><br>Example 4: Use unalias command with name of alias on to remove specific alias command<br><br>1. 1<br><br>1. `unalias cdv`<br><br>Copied!Wrap Toggled!|
|cd|Used to move efficiently from the existing working directory to different directories on your system.|Basic syntax of `cd` command<br><br>1. 1<br><br>1. `cd [options] [directory]`<br><br>Copied!Wrap Toggled!<br><br>Example 1: Change directory location to `folder1`<br><br>1. 1<br><br>1. `cd /usr/local/folder1`<br><br>Copied!Wrap Toggled!<br><br>Example 2: Get back to previous working directory<br><br>1. 1<br><br>1. `cd -`<br><br>Copied!Wrap Toggled!<br><br>Example 3: Move up one level from present working directory tree<br><br>1. 1<br><br>1. `cd ..`<br><br>Copied!Wrap Toggled!|
|dependency-check|OWASP dependency-check is a software composition analysis utility that detects publicly disclosed vulnerabilities in application dependencies.|Basic syntax of `dependency-check` command<br><br>1. 1<br>2. 2<br>3. 3<br>4. 4<br>5. 5<br>6. 6<br><br>1. `dependency-check.bat [options] --scan[directory] --out [directory]`<br><br>3. `` `Options ``<br>4. `project: The name of your project as it should appear in the report`<br>5. `scan [directory]: The folder which contains the 3rd party dependency libraries`<br>6. `` out [directory]: The folder where the vulnerability analysis reports should be exported to` ``<br><br>Copied!Wrap Toggled!<br><br>Example 1: In `Windows`, use command as given below<br><br>1. 1<br><br>1. `dependency-check.bat --project "my_project" --scan "c:\java\application\lib"`<br><br>Copied!Wrap Toggled!<br><br>Example 2: In `Linux`, use command as given below<br><br>1. 1<br><br>1. `dependency-check.sh --project "my_project" --scan "/java/application/lib"`<br><br>Copied!Wrap Toggled!|
|docker network|You can use this code to manage networks. The subcommands can be used to create, inspect, list, remove, prune, connect, and disconnect networks.|Create a `docker network`<br><br>1. 1<br><br>1. `docker network create my_network`<br><br>Copied!Wrap Toggled!<br><br>Verify Network Connection<br><br>1. 1<br><br>1. `docker network inspect my_network`<br><br>Copied!Wrap Toggled!<br><br>List docker Network<br><br>1. 1<br><br>1. `docker network ls`<br><br>Copied!Wrap Toggled!<br><br>Remove docker network<br><br>1. 1<br><br>1. `docker network rm NETWORK_NAME_OR_ID`<br><br>Copied!Wrap Toggled!<br><br>Prune docker network<br><br>1. 1<br><br>1. `docker network prune`<br><br>Copied!Wrap Toggled!<br><br>Connect Docker Network<br><br>1. 1<br><br>1. `docker network connect NETWORK_NAME CONTAINER_NAME_OR_ID`<br><br>Copied!Wrap Toggled!<br><br>Disconnect Docker Network<br><br>1. 1<br><br>1. `docker network disconnect NETWORK_NAME CONTAINER_NAME_OR_ID`<br><br>Copied!Wrap Toggled!|
|docker ps|Lists the running containers by default. We can use different flags to get the list of other containers that are in stopped or exited status.|1. 1<br><br>1. `docker ps [OPTIONS]`<br><br>Copied!Wrap Toggled!<br><br>If you want to see all containers, including the stopped ones, you can use the `-a` or `-all`<br><br>1. 1<br><br>1. `docker ps -a`<br><br>Copied!Wrap Toggled!|
|docker pull|You can download Docker images from the internet.|1. 1<br><br>1. `docker pull [OPTIONS] IMAGE_NAME[:TAG]`<br><br>Copied!Wrap Toggled!|
|docker run|It runs a command in a new container, getting the image and starting the container if needed.|1. 1<br><br>1. `docker run [OPTIONS] IMAGE [COMMAND] [ARG...]`<br><br>Copied!Wrap Toggled!|
|git clone|You can create a copy of a specific repository or branch within a repository.|1. 1<br><br>1. `git clone REPOSITORY_URL [DESTINATION_DIRECTORY]`<br><br>Copied!Wrap Toggled!|
|jake|Jake is a simple JavaScript build program with capabilities similar to the regular make or rake command. It has the following features:<br><br>Jakefiles are in standard JavaScript syntax<br><br>Tasks with prerequisites<br><br>Namespaces for tasks<br><br>Async task execution|1. 1<br><br>1. `jake ddt`<br><br>Copied!Wrap Toggled!|
|jq|Used to transform JSON data into a more readable format and print it to the standard output on Linux.|Basic command syntax<br><br>1. 1<br><br>1. `jq [options] [filter] [file]`<br><br>Copied!Wrap Toggled!<br><br>Let's consider a simple `example.json` file that describes an array as below -<br><br>1. 1<br>2. 2<br>3. 3<br>4. 4<br>5. 5<br>6. 6<br>7. 7<br>8. 8<br>9. 9<br>10. 10<br>11. 11<br>12. 12<br>13. 13<br>14. 14<br>15. 15<br><br>1. `[`<br>2.     `{`<br>3.         `color: "red",`<br>4.         `value: "#f00"`<br>5.     `},`<br>6.     `{`<br>7.         `color: "green",`<br>8.         `value: "#0f0"`<br>9.     `},`<br>10.     `{`<br>11.         `color: "blue",`<br>12.         `value: "#00f"`<br>13.     `}`<br>14. `]`<br><br>Copied!Wrap Toggled!<br><br>Example 1: The identity filter `.` takes the input and produces prints all output unchanged<br><br>1. 1<br><br>1. `jq '.' example.json`<br><br>Copied!Wrap Toggled!<br><br>Example 2: Extract the name of each color from each object in the array<br><br>1. 1<br>2. 2<br><br>1. `jq '.[].color' example.json`<br>2. `jq 'map(has("color"))' example.json`<br><br>Copied!Wrap Toggled!|
|ls|Basic Linux command used for listing information regarding files and directories within the file system.|Basic command syntax<br><br>1. 1<br><br>1. `ls [options] [file/directory]`<br><br>Copied!Wrap Toggled!<br><br>Example 1: Sorts the file names displayed in the order of last modification time. `r` is for displaying in reverse order<br><br>1. 1<br>2. 2<br><br>1. `ls -lt`<br>2. `ls -ltr`<br><br>Copied!Wrap Toggled!<br><br>Example 2: Displays hidden files<br><br>1. 1<br><br>1. `ls -a`<br><br>Copied!Wrap Toggled!|
|pip|To ensure that requests will function, the pip program searches for the package in the Python Package Index (PyPI), resolves any dependencies and installs everything in your current Python environment.|1. 1<br><br>1. `pip list`<br><br>Copied!Wrap Toggled!|
|pip install|The pip install <package> command looks for the latest version of the package and installs it.|1. 1<br><br>1. `pip install example_package`<br><br>Copied!Wrap Toggled!|
|sonar-scanner|The SonarScanner CLI is the scanner to use when there is no specific scanner for your build system.|Basic Syntax of the sonar-scanner command; commonly used options are:<br><br>`-D,--define <arg> Define property`  <br>`-h,--help Display help information`  <br>`-v,--version Display version information`  <br>`-X,--debug Produce execution debug output`<br><br>1. 1<br><br>1. `sonar-scanner [options]`<br><br>Copied!Wrap Toggled!<br><br>Example: verify your installation by executing the command<br><br>1. 1<br><br>1. `sonar-scanner -h`<br><br>Copied!Wrap Toggled!|
|wget|Stands for web get. The `wget` is a free non-interactive file downloader command. Non-interactive means it can work in the background when the user is not logged in.|Basic Syntax of the wget command; commonly used  <br>options are `[-V], [-h], [-b], [-e], [-o], [-a], [-q]`<br><br>1. 1<br><br>1. `wget [options]`<br><br>Copied!Wrap Toggled!<br><br>Example 1: Specifies to `download file.txt` over HTTP website url into the working directory.<br><br>1. 1<br><br>1. `wget http://example.com/file.txt`<br><br>Copied!Wrap Toggled!<br><br>Example 2: Specifies to download the `archive.zip` over HTTP website url in the background and returning you to the command prompt in the interim.<br><br>1. 1<br><br>1. `wget -b http://www.example.org/files/archive.zip`<br><br>Copied!Wrap Toggled!|
|which|Used to locate the executable file associated with the given command by searching it in the path environment variable|Basic syntax of `which` command<br><br>1. 1<br><br>1. `which [option] [filename1] [filename2]`<br><br>Copied!Wrap Toggled!<br><br>Example 1: To know where exactly the java program is located, execute the command as below<br><br>1. 1<br><br>1. `which java`<br><br>Copied!Wrap Toggled!<br><br>Example 2: To know exact location of multiple programs, execute the command as below<br><br>1. 1<br><br>1. `which java python`<br><br>Copied!Wrap Toggled!<br><br>Example 3: By default which command will display the path of the first occurrence, but if we want to display all the occurrences of the program, then we can use `-a` option.<br><br>1. 1<br><br>1. `which -a python`|