|Package/Method|Description|Code Example|
|---|---|---|
|cat|Reads data from the file and gives its content as output. It helps to create, view, and concatenate files.|Example:<br><br>1. 1<br>2. 2<br><br>1. `cat filename`<br>2. `cat file1 file2 file3 ...`<br><br>Copied!Wrap Toggled!|
|cd|Used to move efficiently from your system's existing working directory to different directories.|1. 1<br><br>1. `cd [directory path]`<br><br>Copied!Wrap Toggled!|
|flask run|Can do more than just start the development server. By enabling debug mode, the server will automatically reload if code changes and will show an interactive debugger in the browser if an error occurs during a request. The debugger allows the execution of arbitrary Python code from the browser.|Example:<br><br>1. 1<br>2. 2<br><br>1.  `1.flask run -debug` <br>2.  `2.flask run -debugger #To start your Flask application with debug mode enabled`<br><br>Copied!Wrap Toggled!|
|flask-talisman|Talisman is a small Flask extension that handles setting HTTP headers that can help protect against a few common web application security issues.|Example:<br><br>1. 1<br>2. 2<br>3. 3<br>4. 4<br>5. 5<br>6. 6<br>7. 7<br><br>1. `pip install flask-talisman`<br>2. `from flask import Flask`<br>3. `from flask_talisman import Talisman`<br>4. `app = Flask(__name__)` <br>5. `# Configure the security policies`<br>6. `talisman = Talisman(app)`<br>7. `# ... Your other Flask app code ...`<br><br>Copied!Wrap Toggled!|
|git clone|You can create a copy of a specific repository or branch within a repository.|1. 1<br><br>1. `git clone REPOSITORY_URL [DESTINATION_DIRECTORY]`<br><br>Copied!Wrap Toggled!|
|gpg --full-generate-key|Generates a new GPG key to use for signing commits and tags.|Syntax:<br><br>1. 1<br><br>1. `gpg --full-generate-key`<br><br>Copied!Wrap Toggled!|
|gpg --list-secret-keys|Lists the long form of the GPG keys for which you have both a public and private key. A private key is required for signing commits or tags.|Syntax:<br><br>1. 1<br><br>1. `gpg --list-keys` <br><br>Copied!Wrap Toggled!|
|grep|Searches for the pattern specified by the pattern parameter and writes each matching line to standard output.|Syntax:<br><br>1. 1<br><br>1. `grep [options] pattern [file ...]`<br><br>Copied!Wrap Toggled!<br><br>Examples:<br><br>1. 1<br>2. 2<br>3. 3<br><br>1.  `grep "searchword" filename`<br>2.  `grep -i "pattern" filename #-i: Perform a case-insensitive search.`<br>3.  `grep "pattern" file1 file2 file3 #pattern: The text pattern you want to search for.`<br><br>Copied!Wrap Toggled!|
|npm start|Runs a predefined command specified in the "start" property of a package's "scripts" object. If the "scripts" object does not define a "start" property, npm will run the node server.|Syntax:<br><br>1. 1<br>2. 2<br><br>1. `npm start  #Runs in Terminal` <br><br>Copied!Wrap Toggled!<br><br>Example for Node.js:<br><br>1. 1<br>2. 2<br>3. 3<br>4. 4<br>5. 5<br>6. 6<br><br>1.  `{`<br>2.     `"name": "my-node-app,ࠠ   "version": "1.0.0",`<br>3.     `"scripts": {`<br>4.    `"start": "node server.js"`<br>5.  `}`<br>6. `}`<br><br>Copied!Wrap Toggled!|
|pass|With the pass, each password lives inside of a gpg encrypted file whose filename is the title of the website or resource that requires the password.|1. 1<br><br>1. `pass`<br><br>Copied!Wrap Toggled!|
|pass init|Initiate your pass directory (.password-store) within your home folder. Pass will store your passwords here.|Syntax:<br><br>1. 1<br><br>1.   `pass init`		<br><br>Copied!Wrap Toggled!<br><br>You need to set up GPG and initialize the password store.|
|pass insert|Passwords are added to the store using this command. This accepts the name of the service as an argument and interactively prompts you to enter the password.|Storing Passwords:<br><br>1. 1<br><br>1. `pass insert example.com`<br><br>Copied!Wrap Toggled!<br><br>You use the pass insert command followed by the `title(filename)` of the password and the password itself.|
|pass show|Fetches a password from the store/directory.|Syntax:<br><br>1. 1<br><br>1. `pass show example.com`<br><br>Copied!Wrap Toggled!<br><br>For example, if you have a password stored for a website named `example.com` running pass show `example.com` will show the decrypted password in the terminal.|
|pip|To ensure that requests will function, the pip program searches for the package in the Python Package Index (PyPI), resolves any dependencies and installs everything in your current Python environment.|1. 1<br><br>1. `pip list`<br><br>Copied!Wrap Toggled!|
|pip install|The pip install <package> command looks for the latest version of the package and installs it.|1. 1<br><br>1. `pip install example_package`         <br><br>Copied!Wrap Toggled!|
|rm|Used to remove objects such as files, directories, symbolic links, and so on from the file system.|Examples:<br><br>1. 1<br>2. 2<br><br>1. `rm filename`<br>2. `rm file1 file2 file3`<br><br>Copied!Wrap Toggled!|
|sudo apt|sudo apt-get update and sudo apt-get upgrade are two commands you can use to keep all of your packages up to date in Debian or a Debian-based Linux distribution.|Syntax:<br><br>1. 1<br>2. 2<br>3. 3<br>4. 4<br>5. 5<br><br>1. `sudo apt update`<br>2. `or`<br>3. `sudo apt-get update`<br>4. `or`<br>5. `sudo apt-get update && sudo apt-get upgrade`<br><br>Copied!Wrap Toggled!|
|wget|Stands for web get. The wget is a free non-interactive file downloader command. Non-interactive means it can work in the background when the user is not logged in.|1. 1<br><br>1. `wget 'https://www.example.com/somefile.txt'`|