```sql
SELECT name, salary FROM employee

WHERE salary = SELECT MAX(salary) 
FROM employee
// this is the innermost query used to select the max salary

WHERE salary < (SELECT MAX(salary) 
FROM employee));
```
