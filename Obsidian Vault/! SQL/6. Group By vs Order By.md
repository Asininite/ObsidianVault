## Group BY
- putting your data into buckets
- used with **aggregate functions** like SUM AVG COUNT
	  to calculate total salary
	  average salary
	  count of employees in each department
	  
```sql
SELECT department, AVG(salary)
FROM employees
GROUP BY department;
```
![[Pasted image 20250910220837.png]]
