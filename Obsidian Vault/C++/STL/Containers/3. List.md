```cpp
list<int> ls;

	ls.push_back(2); | adds 2 
	ls.emplace_back(3); | adds 3 dynamically
	ls.push_front(4); | adds 5 to front of the list {5, 2, 3}
	ls.emplace_front(); 

rest similar to vector
```